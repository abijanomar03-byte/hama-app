# Hama publishing flow

New listings are created as `pending` and are not visible in the public feed until a reviewer changes the property status to `active` in Supabase.

AI photo verification is intentionally disabled for the launch MVP. Captured media is accepted immediately and manually reviewed before publication.

For media, the user chooses exactly one method: required room photos or one walkthrough video.
